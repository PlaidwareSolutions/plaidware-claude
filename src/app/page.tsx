// M1 placeholder — the real marketing site lands in M2.
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 px-6">
      <p className="text-xs font-semibold uppercase tracking-[0.22em] text-coral">
        Accelerating business throughput
      </p>
      <h1 className="text-center text-4xl font-bold tracking-tight text-heading">
        Plaidware
      </h1>
      <p className="max-w-md text-center text-muted">
        One control plane for every Plaidware product. The new Hub is under
        construction — foundation milestone M1.
      </p>
    </main>
  );
}
